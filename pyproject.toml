[project]
name = "agents-updater"
version = "0.1.1"
description = "CLI tool to update AGENTS.md from a central repository"
license = { text = "MIT" }
readme = "README.md"
requires-python = ">=3.9"

[project.scripts]
agents-update = "agents_updater:main"
